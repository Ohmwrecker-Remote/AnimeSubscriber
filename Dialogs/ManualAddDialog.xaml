<Window x:Class="AnimeSubscriber.Dialogs.ManualAddDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="手动添加订阅" Width="440" Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BgCardBrush}"
        FontFamily="{StaticResource BodyFont}"
        FontSize="{StaticResource NormalFontSize}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="番剧名称：" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,4"/>
        <TextBox x:Name="NameBox" Grid.Row="1"
                 Style="{StaticResource ThemedTextBox}" Height="30"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="取消" Style="{StaticResource SecondaryButton}" Width="80" Height="30"
                    IsCancel="True" Margin="0,0,8,0"/>
            <Button Content="确定" Style="{StaticResource CoralButton}" Width="80" Height="30"
                    IsDefault="True" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
